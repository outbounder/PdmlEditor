<?xml version="1.0" encoding="utf-8"?>
<s:Group xmlns:fx="http://ns.adobe.com/mxml/2009" 
		xmlns:s="library://ns.adobe.com/flex/spark" 
		xmlns:mx="library://ns.adobe.com/flex/halo">
	
	<fx:Script>
		<![CDATA[
			import components.modules.renderers.ModuleRenderer;
			
			import mx.collections.ArrayCollection;
			
			[Bindable]
			public var modulesList:ArrayCollection;
			
			/* private function onMouseDown(event:MouseEvent):void
			{
				if(this.modulesListView.selectedIndex == -1)
					return;
				var dragSource:DragSource = new DragSource();
				dragSource.addData(this.modulesListView.selectedItem, "module");
				DragManager.doDrag(this.modulesListView.dataGroup.getElementAt(this.modulesListView.selectedIndex) as UIComponent, 
						dragSource, event);
			} */
		]]>
	</fx:Script>
	
	<s:List id="modulesListView" height="100%" width="100%"
			dataProvider="{this.modulesList}"
			itemRenderer="{new ClassFactory(ModuleRenderer)}"/>
	
</s:Group>
